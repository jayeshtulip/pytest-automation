# requirements.txt

# Testing
pytest==6.2.4

# Database connectivity
psycopg2-binary==2.9.1

# Nmap IP Validation
# Assuming nmap_ip_validation is the module or library you are using
# Replace with the correct package name if it differs
# Also, specify the version if possible
# Example: nmap_ip_validation==1.0.0

# Other dependencies
# Add any other dependencies your code relies on
attrs==20.3.0

